generate genesis block
add ethereum address
export json file

initialize 2 nodes on the new chain
activate a miner node
use enode parameter to set-up second node that will sync to the blockchain

input mnemonic seed into MyCrypto wallet
set-up custom MyCrypto node with localhost port
use private key to access custom node address

input a recipient's address
set fee
confirm the transaction
check the transaction status

